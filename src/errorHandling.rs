

enum Result<T,E> {
    Ok(T),
    Err(E)
}

fn error_handling() {
    // let result = fs::read_to_string("the_ultimate_question.txt");
    // let content = match result {
    //     Ok(message) => message,
    //     Err(error) => String::from("Nobody knoe the file")
    // };
    // println!("content is {}",content);
}
